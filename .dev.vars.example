# Self-hosted Moltbot Worker - Required Secrets
# These values will be prompted during Deploy Button setup

# Tunnel internal URL (format: https://<TUNNEL_ID>.cfargotunnel.com)
SELFHOSTED_URL=https://xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx.cfargotunnel.com

# Gateway access token (must match your server's MOLTBOT_GATEWAY_TOKEN)
MOLTBOT_GATEWAY_TOKEN=your-gateway-token

# Cloudflare Access configuration
CF_ACCESS_TEAM_DOMAIN=myteam.cloudflareaccess.com
CF_ACCESS_AUD=your-access-application-aud-tag

# Optional: Internal auth token for additional security
# SELFHOSTED_AUTH_TOKEN=your-internal-auth-token

# Optional: CDP secret for browser automation
# CDP_SECRET=your-cdp-secret

# Optional: Enable debug routes
# DEBUG_ROUTES=true
